# Server Configuration
NODE_ENV=development
PORT=5000

# Database
MONGO_URI=mongodb://localhost:27017/faz-football
DB_HOST=localhost
DB_PORT=27017
DB_NAME=faz-football

# JWT
JWT_SECRET=your-secret-key-change-this-in-production

# Cloudinary
CLOUDINARY_CLOUD_NAME=your-cloudinary-cloud-name
CLOUDINARY_API_KEY=your-cloudinary-api-key
CLOUDINARY_API_SECRET=your-cloudinary-api-secret
CLOUDINARY_FOLDER=faz/clubs

# Email/SMTP Configuration (choose one)
# Option 1: Generic SMTP
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_SECURE=false
MAIL_FROM=noreply@faz-football.com

# Option 2: AWS SES (uncomment if using SES instead)
# SES_REGION=us-east-1
# SES_SMTP_HOST=email-smtp.us-east-1.amazonaws.com
# SES_SMTP_PORT=587
# SES_SMTP_USER=your-ses-smtp-user
# SES_SMTP_PASS=your-ses-smtp-password

# Frontend URLs
FRONTEND_URL=https://autonomyzambia.online
FRONTEND_URL_DEV=http://localhost:5173
FRONTEND_ORIGINS=http://localhost:5173,http://localhost:5174,http://localhost:5175

# Club Registration
CLUBS_REQUIRE_SIGNUP_TOKEN=false
